name: ativ_04  # Replace with your project name
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"  # Adjust the SDK version as needed

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4  # Check for the latest version on pub.dev
  path: ^1.8.0      # Check for the latest version on pub.dev

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

# Add any additional assets or configurations here